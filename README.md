# OutMarketThem

Marketing website for OutMarketThem — web design and marketing rescue for trades businesses in the US and Australia.

**Live domain:** outmarketthem.com

**Entities:**
- OutMarketThem LLC — Delaware, USA
- EHUSTLE PTY LTD — Queensland, Australia — ABN 21 679 259 440

---

## Stack

- Next.js 14 (App Router)
- Tailwind CSS (warm cream/clay/teal palette)
- Fraunces (serif, display) + Inter (sans, body) via `next/font`
- Lucide React icons
- Deployed on Netlify via `@netlify/plugin-nextjs`

---

## Getting started

```bash
npm install
npm run dev
```

Visit [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run build
npm run start
```

---

## Pages

| Route          | Purpose                                         |
| -------------- | ----------------------------------------------- |
| `/`            | Home — hero, services overview, testimonials    |
| `/websites`    | $997 website build — pricing, process, FAQ      |
| `/rescue`      | Google Ads / Meta Ads / SEO rescues + bundle    |
| `/advisory`    | $99/mo weekly group calls                       |
| `/about`       | Founder story, values, AU + US entities         |
| `/work`        | Filterable portfolio (AU / USA / trade)         |
| `/contact`     | Deposit CTA + contact form                      |
| `/thank-you`   | Post-deposit confirmation + next-steps list     |
| `/privacy`     | Privacy policy (covers AU + US)                 |
| `/terms`       | Terms of service                                |
| `/refunds`     | Refund policy                                   |
| `/*`           | 404 page                                        |

Sitemap and robots.txt are generated automatically (`app/sitemap.ts`, `app/robots.ts`).

---

## Design tokens

Defined in `tailwind.config.ts`:

| Token         | Hex        | Role                       |
| ------------- | ---------- | -------------------------- |
| `cream`       | `#FAF6F0`  | Page background            |
| `cream-card`  | `#FFFFFF`  | Card and panel background  |
| `ink`         | `#2A2520`  | Primary text               |
| `muted`       | `#5A5249`  | Secondary text             |
| `clay`        | `#E07856`  | Primary accent / CTAs      |
| `teal`        | `#2D5F5D`  | Secondary accent           |
| `sage`        | `#5A8A5B`  | Success / positive         |
| `amber`       | `#F4D47C`  | Warm highlight             |
| `border`      | `#E8DFD3`  | Borders / dividers         |

---

## Assets to replace before launch

Everything ships as placeholder art so the site is production-visual immediately. Drop real assets into `/public/images/` and update `PlaceholderImage` usages to `next/image`:

- `public/images/ben-portrait.jpg` — founder photo (home, about)
- `public/images/work/*.jpg` — real site screenshots (home, work, websites)
- `public/og-default.png` — replace `.svg` with `1200x630` PNG for full OG support
- `public/apple-touch-icon.png` — replace `.svg` with `180x180` PNG (iOS doesn't rasterise SVG touch icons in all versions)

The `PlaceholderImage` component is in `components/PlaceholderImage.tsx`. Search for its usages to find each replacement point.

---

## Third-party wiring

| Service     | Where                                        | TODO                                         |
| ----------- | -------------------------------------------- | -------------------------------------------- |
| Formspree   | Contact form `action` attribute              | Replace `REPLACE_WITH_FORM_ID`               |
| Stripe      | Deposit CTAs                                 | Replace `buy.stripe.com/test_deposit_placeholder` with real Payment Link |
| GA4 / GTM   | `app/layout.tsx`                             | Add Google Tag container when ready          |

---

## Deployment

Netlify is preconfigured via `netlify.toml`:

1. Push the repo to GitHub
2. Connect the repo in Netlify
3. Netlify auto-detects Next.js and uses the Next.js runtime plugin
4. Set custom domain `outmarketthem.com`

No environment variables are required for the default build.

---

## Performance & SEO

- Fonts use `next/font` with `display: swap` (no FOUT, no blocking)
- No client JS on static pages except the nav and FAQ (interactive)
- Every page has its own `metadata` (title + description)
- JSON-LD schema for `Organization`, `ProfessionalService`, `Service`, `LocalBusiness` on relevant pages
- `sitemap.xml` and `robots.txt` generated by Next.js route handlers

---

## Content tone rules (in case you edit)

- Direct, honest, warm
- Short sentences, active voice
- No corporate jargon ("synergies", "leverage", "solutions")
- No AI tells (em dashes used sparingly, no "it's not just X, it's Y" constructions)
- First person ("I") where appropriate — this is one person, not a team
- Specific numbers, not vague claims
- Treats the reader as intelligent
